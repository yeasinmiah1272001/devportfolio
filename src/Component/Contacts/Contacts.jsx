import Container from "../Container/Container";

const Contacts = () => {
      return (
            <div>
                  <Container id={"contact"} className="bg-purple-600">Contacts</Container>
            </div>
      );
};

export default Contacts;